# Thousands Minigames
# Instance_Startup_Scripts_Generator 配置
# 相对路径相对于本 TOML 文件所在目录解析。

# 启动脚本输出目录。目录路径。
output_dir = "..\\..\\_startup_scripts_legacy\\"

# 实例配置根目录。目录路径。
configs_root = "..\\..\\configs\\"

# 世界根目录。目录路径。
worlds_root = "..\\..\\worlds\\"

# 版本映射文件。文件路径。
version_map = ".\\Version_Map.toml"

# Java 运行时路径。原样嵌入 .bat 。
java_runtime = ".\\runtimes\\zulu-25\\bin\\java.exe"

# JVM 初始内存。
xms = "6144M"

# JVM 最大内存。
xmx = "6144M"

# 启动脚本备份目录。目录路径。
backup_dir = "..\\..\\_startup_scripts_legacy\\_backups\\"
